@model Rockend.iStrata.StrataWebsite.Model.MessageModel
<h1>
    @ViewData.Model.Title</h1>
<div class="full-width-data-container">
    @ViewData.Model.Message
    @if (!string.IsNullOrEmpty(ViewData.Model.ReturnUrl))
    { 
        <br />
        <a class="coolButton" href="@ViewData.Model.ReturnUrl">OK</a>
    }
</div>