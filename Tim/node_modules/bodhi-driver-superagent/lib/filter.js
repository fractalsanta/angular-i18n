(function(exports) {
    var util = require('util');
    exports.ResourceFilter = function ResourceFilter(type) {
        var filter = this;
        var res = ['resources', type].join('/');
        filter.toUri = function (criteria) {
            return util.format('%s?where=%s', res, JSON.stringify(criteria));
        }
    }
})(module.exports);